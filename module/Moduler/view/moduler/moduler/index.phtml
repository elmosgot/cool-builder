<?php
$form->prepare();
echo $this->form()->openTag( $form );
$elements = array( 'template', 'name', 'table', 'unit', 'project', 'csrf', 'build' );
echo $this->renderElements( $form, $elements );
?>
</form>
<ul>
<?php
foreach( $log as $message ) {
	echo sprintf( '<li>%s</li>', $message );
}
 ?>
</ul>
